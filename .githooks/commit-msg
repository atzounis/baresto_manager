#!/bin/sh
# Strip Cursor co-author trailers so cursoragent is not credited on GitHub.
# Enable: git config core.hooksPath .githooks
grep -viq 'Co-authored-by:.*cursoragent' "$1" && exit 0
grep -vi 'Co-authored-by:.*cursoragent' "$1" > "$1.tmp" && mv "$1.tmp" "$1"
